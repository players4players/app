import auth from '../store/auth';
import people from '../store/people';
import theme from '../store/theme';

// Store map consumed by the shared @store runtime.
export default {
  auth,
  people,
  theme,
};
